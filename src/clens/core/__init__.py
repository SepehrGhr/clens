"""Language-agnostic core: source positions, tokens, diagnostics, AST base
types, the generic lexer/parser engines, and highlighting primitives.

Nothing in this package may import from ``clens.languages``.
"""
