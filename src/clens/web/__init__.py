"""The web UI (S8.2, D22): a stdlib `http.server` backend plus a vanilla-JS
front end. Satisfies the course document's Phase 3 interface requirement
(§6.6 lists Web UI as one of three acceptable options) — pulled forward
rather than extra work.
"""
