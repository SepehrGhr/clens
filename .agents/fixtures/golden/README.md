# Golden expectations

Taken directly from the course document. These are exact and are not to be
regenerated from our own output — they are the specification.

- `factorial_tokens.txt`  — course document, section 4.2, abbreviated token stream
- `factorial_ast.txt`     — course document, section 4.3.2, with exact locations
- `invalid_char.txt`      — course document, section 4.2.3 example

Other golden files under `tests/golden/expected/` (ANSI output, HTML output, JSON
diagnostics) are ours and may be regenerated — but always eyeball a regenerated
file before committing it.
